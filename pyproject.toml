[project]
name = "tytorch"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "SaferSephy", email = "t.vandervelden3@student.han.nl" }
]
dependencies = [
    "loguru",
    "pydantic",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "ipykernel",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/tytorch"]
